文件格式说明：
第 1 行：16 22106 83718 → 相机数、点数、观测条数。

接下来 83718 行：每条观测一行：相机id 点id u v。

从第 83720 行起（紧接在最后一行观测之后）：不再有相机/点编号，而是 连续一共 9×16 + 3×22106 = 66462 个浮点数：

先 16 个相机，每个相机 9 个数（角轴 3 + 后面 6 维，与 Pose/内参等在数据里的打包方式一致）；

9个数的含义：
0–2	角轴（Rodrigues，3 维）表示旋转 (R)
3–5	平移 (t)（与 AngleAxisRotatePoint 后相加，再投影）
6	焦距 (f)（假设 (f_x=f_y)，共用一个尺度）
7–8	径向畸变的两项系数（注释里写的是对 (r^2) 的二次与四次项形式：1 + r²(l1 + l2·r²)）

每个观测点在全局坐标系下的三维坐标（世界坐标）：
再 22106 个点，每个点 3 个数 ((x,y,z))

前面是「谁看见了谁、像素是多少」；后面是「当前给定的相机参数和 3D 坐标初值」，所以格式变成纯数字列表。